function anwserbot(question)
let question = prompt "dis moi un truc"; {
    if(question[question.length-1] === "?") {
        return "Ouais ouais ... "
    }
    if(question[question.length] === toUpperCase )
    return "pwa calme toi !"
}